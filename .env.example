# VITE_API_BASE_URL: Frontend API base path.
# Use a full URL when connecting to a separate backend in local development.
# In same-origin deployments, "/api" is also supported by the frontend client fallback.
VITE_API_BASE_URL="http://localhost:8080"
